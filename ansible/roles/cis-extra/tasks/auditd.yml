---
# ── CIS 4.1 — auditd configuration ────────────────────────────────────────
# oscap applies most audit rules. We ensure the daemon config is sane
# and add supplemental rules for completeness.

- name: "CIS 4.1.1.1 | Ensure auditd is installed"
  ansible.builtin.package:
    name:  audit
    state: present

- name: "CIS 4.1.1.2 | Ensure auditd service is enabled and running"
  ansible.builtin.service:
    name:    auditd
    enabled: true
    state:   started

- name: "CIS 4.1.1.3 | Configure auditd.conf"
  ansible.builtin.lineinfile:
    path:   /etc/audit/auditd.conf
    regexp: "^{{ item.key }}\\s*="
    line:   "{{ item.key }} = {{ item.value }}"
    state:  present
  loop:
    - { key: max_log_file,         value: "{{ auditd_max_log_file }}" }
    - { key: num_logs,             value: "{{ auditd_num_logs }}" }
    - { key: max_log_file_action,  value: "{{ auditd_max_log_file_action }}" }
    - { key: space_left_action,    value: "email" }
    - { key: action_mail_acct,     value: "root" }
    - { key: admin_space_left_action, value: "halt" }
  notify: restart auditd

- name: "CIS 4.1.3 | Ensure audit log directory permissions"
  ansible.builtin.file:
    path:  /var/log/audit
    owner: root
    group: root
    mode:  '0700'
    state: directory

- name: "CIS 4.1 | Deploy supplemental audit rules"
  ansible.builtin.copy:
    dest: /etc/audit/rules.d/99-cis-extra.rules
    owner: root
    group: root
    mode: '0640'
    content: |
      ## CIS supplemental audit rules
      ## Generated by cis-extra Ansible role

      # CIS 4.1.4 — Record events that modify date/time
      -a always,exit -F arch=b64 -S adjtimex -S settimeofday -k time-change
      -a always,exit -F arch=b32 -S adjtimex -S settimeofday -S stime -k time-change
      -a always,exit -F arch=b64 -S clock_settime -k time-change
      -a always,exit -F arch=b32 -S clock_settime -k time-change
      -w /etc/localtime -p wa -k time-change

      # CIS 4.1.5 — Record events that modify user/group information
      -w /etc/group    -p wa -k identity
      -w /etc/passwd   -p wa -k identity
      -w /etc/gshadow  -p wa -k identity
      -w /etc/shadow   -p wa -k identity
      -w /etc/security/opasswd -p wa -k identity

      # CIS 4.1.6 — Record events that modify network environment
      -a always,exit -F arch=b64 -S sethostname -S setdomainname -k system-locale
      -a always,exit -F arch=b32 -S sethostname -S setdomainname -k system-locale
      -w /etc/issue     -p wa -k system-locale
      -w /etc/issue.net -p wa -k system-locale
      -w /etc/hosts     -p wa -k system-locale
      -w /etc/sysconfig/network -p wa -k system-locale

      # CIS 4.1.7 — Record events that modify MAC policy
      -w /etc/selinux/ -p wa -k MAC-policy

      # CIS 4.1.8 — Record login/logout events
      -w /var/log/faillog  -p wa -k logins
      -w /var/log/lastlog  -p wa -k logins
      -w /var/log/tallylog -p wa -k logins

      # CIS 4.1.9 — Record session initiation events
      -w /var/run/utmp  -p wa -k session
      -w /var/log/wtmp  -p wa -k logins
      -w /var/log/btmp  -p wa -k logins

      # CIS 4.1.10 — Record permission changes
      -a always,exit -F arch=b64 -S chmod -S fchmod -S fchmodat -F auid>=1000 -F auid!=4294967295 -k perm_mod
      -a always,exit -F arch=b32 -S chmod -S fchmod -S fchmodat -F auid>=1000 -F auid!=4294967295 -k perm_mod
      -a always,exit -F arch=b64 -S chown -S fchown -S fchownat -S lchown -F auid>=1000 -F auid!=4294967295 -k perm_mod
      -a always,exit -F arch=b32 -S chown -S fchown -S fchownat -S lchown -F auid>=1000 -F auid!=4294967295 -k perm_mod
      -a always,exit -F arch=b64 -S setxattr -S lsetxattr -S fsetxattr -S removexattr -S lremovexattr -S fremovexattr -F auid>=1000 -F auid!=4294967295 -k perm_mod
      -a always,exit -F arch=b32 -S setxattr -S lsetxattr -S fsetxattr -S removexattr -S lremovexattr -S fremovexattr -F auid>=1000 -F auid!=4294967295 -k perm_mod

      # CIS 4.1.11 — Record unsuccessful file access
      -a always,exit -F arch=b64 -S creat -S open -S openat -S truncate -S ftruncate -F exit=-EACCES -F auid>=1000 -F auid!=4294967295 -k access
      -a always,exit -F arch=b32 -S creat -S open -S openat -S truncate -S ftruncate -F exit=-EACCES -F auid>=1000 -F auid!=4294967295 -k access
      -a always,exit -F arch=b64 -S creat -S open -S openat -S truncate -S ftruncate -F exit=-EPERM  -F auid>=1000 -F auid!=4294967295 -k access
      -a always,exit -F arch=b32 -S creat -S open -S openat -S truncate -S ftruncate -F exit=-EPERM  -F auid>=1000 -F auid!=4294967295 -k access

      # CIS 4.1.13 — Record successful mounts
      -a always,exit -F arch=b64 -S mount -F auid>=1000 -F auid!=4294967295 -k mounts
      -a always,exit -F arch=b32 -S mount -F auid>=1000 -F auid!=4294967295 -k mounts

      # CIS 4.1.14 — Record file deletion events
      -a always,exit -F arch=b64 -S unlink -S unlinkat -S rename -S renameat -F auid>=1000 -F auid!=4294967295 -k delete
      -a always,exit -F arch=b32 -S unlink -S unlinkat -S rename -S renameat -F auid>=1000 -F auid!=4294967295 -k delete

      # CIS 4.1.15 — Collect changes to sudoers files
      -w /etc/sudoers      -p wa -k scope
      -w /etc/sudoers.d/   -p wa -k scope

      # CIS 4.1.16 — Collect system administrator actions (sudolog)
      -w /var/log/sudo.log -p wa -k actions

      # CIS 4.1.17 — Collect kernel module loading/unloading
      -w /sbin/insmod  -p x -k modules
      -w /sbin/rmmod   -p x -k modules
      -w /sbin/modprobe -p x -k modules
      -a always,exit -F arch=b64 -S init_module -S delete_module -k modules

      # Make audit configuration immutable (CIS 4.1.18)
      # NOTE: Comment this out during development — requires reboot to change rules.
      # -e 2
  notify: restart auditd

- name: "CIS 4.2.1 | Ensure rsyslog service is enabled"
  ansible.builtin.service:
    name:    rsyslog
    enabled: true
    state:   started
